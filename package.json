{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "engines": {
    "node": ">=14.16.0",
    "npm": ">=6.14.11",
    "git": ">=2.31.1"
  },
  "scripts": {
    "all": "npm run build-lib && npm run build && cls && npm run launch",
    "build": "lerna run build-sandbox",
    "build-lib": "lerna run build-lib",
    "launch": "lerna run launch --stream"
  }
}
